#include <bits/stdc++.h>

using namespace std;


int main() {
    int n = (cin >> (cout << "give vector size: ", n), n);

    vector<int> arr(n);

    for (int i = 0; i < arr.size(); i++) {
        cin >> arr[i];
    }

    return 0;
}
